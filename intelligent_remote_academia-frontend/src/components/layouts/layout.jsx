import React from "react";
import Sidebar from "./sidebar/sidebar";

const Layout = (props) => {
  return (
    <React.Fragment>
      <Sidebar />
    </React.Fragment>
  );
};

export default Layout;
