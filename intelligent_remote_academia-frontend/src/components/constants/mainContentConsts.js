import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  container: {
    marginTop: "40px",
    padding: "40px 20px",
  },

  menu: {
    width: "1500px",
  },
});

export { useStyles };
