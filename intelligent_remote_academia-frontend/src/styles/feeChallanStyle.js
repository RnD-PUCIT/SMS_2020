import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    marginTop: "20px",
  },
  paper: {
    padding: "40px",
  },
  divider: {
    margin: "20px 0",
  },
});

export default useStyles;
